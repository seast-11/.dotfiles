if [ -d "$HOME/.bin" ] ;
  then PATH="$HOME/.bin:$PATH"
fi

if [ -d "$HOME/.local/bin" ] ;
  then PATH="$HOME/.local/bin:$PATH"
fi

/* POPOS ONLY 
/* if [ -d "$HOME/.local/bin/OmniSharp" ] ; */
/*   then PATH="$HOME/.local/bin/OmniSharp:$PATH" */
/* fi */
/*  */
/* if [ -d "$HOME/.local/bin/lua-language-server/bin" ] ; */
/*   then PATH="$HOME/.local/bin/lua-language-server/bin:$PATH" */
/* fi */

if [ -d "$HOME/Applications" ] ;
  then PATH="$HOME/Applications:$PATH"
fi


